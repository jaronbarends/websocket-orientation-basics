{
  "name": "websocket-orientation-basics",
  "version": "0.0.1",
  "description": "remote devices with sockets",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^2.1.1"
  },
  "engines": {
    "node": "0.12.x"
  },
  "scripts": {
    "preinstall": "npm install --save socket.io --msvs_version=2013",
    "start": "node socket-server.js"
  }
}
